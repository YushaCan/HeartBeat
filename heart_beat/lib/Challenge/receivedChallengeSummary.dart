import 'package:flutter/cupertino.dart';

class receivedChallengeSummary extends StatelessWidget {
  const receivedChallengeSummary({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
